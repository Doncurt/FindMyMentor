<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <h1 class="lead">{{mentorPostsp.name}}</h1>
    <p>{{mentorPosts.summary}}</p>
    <p>{{mentorPosts.interest1}}</p>
    <p>{{mentorPosts.socialLink1 }}</p>
  </div>
  <div class="col-sm-6 col-sm-offset-3">
  <form method="POST" action="/mentor-posts/{{mentorPosts._id}}/menteeReply">
    <label for="comment-content"> Request a Mentor </label>
    <textarea name="content" id="menteeReply-content" class='form-control' placeholder="Comment"></textarea>
    <div class="text-right">
      <button type="submit">Request Me!</button>
    </div>
  </form>
  <div>
    <p>Mentee Replies:</p>
    {{#each mentorPost.menteeReply}}
      <li class="list-group-item">
        <p>Submitted by {{this.author.username}}<p>
        <a href="/menteeReplies/{{this._id}}/new">Comment</a>
        <p>{{this.content}}</p>
        {{#if this.menteeReply}}
          {{> menteeReply}}
        {{/if}}
      </li>
    {{/each}}
  </div>

</div>
