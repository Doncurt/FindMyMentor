<div class="row">
  {{#mentorPosts}}
  <div class="col-sm-8 col-sm-offset-2" style="margin:20px; margin-bottom: 0px;">
    <li class="list-group-item">
      <a href="/mentor-posts/{{this._id}}" class='lead'>{{this.name}}</a>
      </br>
      <p>{{this.summary}}<p>
    </li>
  </div>
  {{/mentorPosts}}
  {{#unless mentorPosts}}
    <h3> You're  the first mentor here!</h3>
  {{/unless}}
</div>
